/**
 * jbpm4_deployment		存储流程定义的部署信息，每部署一次插入1条记录
 * jbpm4_deployprop		存储流程定义对应的基本信息：langid-流程定义使用的语言，pdkey+pdversion=pdid-流程定义的id
 * jbpm4_lob 			存储流程定义文件的字符串 
 
 	jbpm4_execution 	存储1个完整的业务流程，当本业务流程全部流转完成后，此记录将被删除---当前正在流转的流程，全部完成后将被删除！
 	jbpm4_task			存储当前业务流程流转到哪个任务节点，当该节点的任务被审批后，会删除此任务记录，同时插入下一个任务记录(如果有下一个任务节点)---当前需要被处理的任务，完成后将被删除！
	
	jbpm4_hist_procinst	存放整个业务流程审核的历史任务基本信息
	jbpm4_hist_task		存放历史任务的每个节点的审批信息 
 */