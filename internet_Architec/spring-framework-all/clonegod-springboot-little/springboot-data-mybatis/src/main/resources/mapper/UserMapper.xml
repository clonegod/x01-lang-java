<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clonegod.mybatis.mapper.UserMapper">
    
    <select id="getUserNameById" resultType="string">
        SELECT name FROM t_user 
        	WHERE id = #{id}
    </select>
    
</mapper>